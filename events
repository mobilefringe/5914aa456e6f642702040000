<div class="container-fluid trim_screen" style="padding-top:25px;">    
    <div id="events_container"> 
        <scirpt id="events_template" type="x-tmpl-mustache/text">
            <div class="row-fluid span12">            
                <div class="span3">
                    <a href="/events/{{slug}}">
                        <img src="{{logo}}" />   
                    </a>
                </div>
                <div class="span9">
                    <h3><a href="/events/{{slug}}">{{name}}</a></h3>    			                
            	    {{{rich_description}}}
                    <a href="/events/{{slug}}" class="read_more">Read More</a>
                </div>        
            </div>
            <div class="line_break">&nbsp;</div>
        </scirpt>
    </div>
    <div class="store_desc hidden_now" id="no_events">
        <h3>
            Currently we have no Events, but check back soon!
        </h3>
    </div>
</div>


<script>
    $(document).ready(function(e){
        init(e);
        loadMallData(renderAll); 
    })
    
    function renderAll(){
        var events = getEventsList().sortBy(function(o){ return o.start_date });
        var published_events = [];
        $.each(events,function(key,val){
            today = moment();
            webDate = moment(val.show_on_web_date);
            if(today>=webDate) {
                published_events.push(val);
            }
        });
        if(published_events.length > 0) {
            renderEvents("#events_container","#events_template",published_events);
            $('#events_container').hide();
        } else {
            // $('#no_events').show();
        }
 
        show_content();
    } 
</script>